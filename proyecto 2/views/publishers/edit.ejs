<%- include('../template/head', { titleWeb: 'Actulizar' , cssStyle: 'css/edit.css' }) %>

    <%- include('../template/navbar') %>

        <div class="container-fluid">
            <div class="container my-5">
                <h2 class="text-center mb-4 text-warning fw-bold">Editar Información de la Editorial</h2>
                <p class="text-center text-muted mb-4">Modifica el nombre de la editorial y guarda los cambios.</p>

                <form action="/publishers/update/<%= publisher.id_publisher %>" method="post"
                    class="p-4 bg-light rounded shadow-sm border border-warning-subtle">
                    <div class="row g-3">

                        <!-- Nombre de la Editorial -->
                        <div class="col-12">
                            <label for="name" class="form-label fw-semibold text-warning">Nombre de la Editorial</label>
                            <div class="input-group">
                                <span class="input-group-text bg-warning text-dark"><i
                                        class="bi bi-building"></i></span>
                                <input type="text" class="form-control" id="name" name="name"
                                    value="<%= publisher.name %>" required placeholder="Ej. Penguin Random House">
                            </div>
                        </div>

                        <!-- Estado -->
                        <div class="col-md-6">
                            <label for="state" class="form-label fw-semibold text-warning">Estado</label>
                            <select class="form-select border-warning-subtle" id="state" name="state" required>
                                <option value="1" <%=publisher.state==1 ? 'selected' : '' %>>Activo</option>
                                <option value="0" <%=publisher.state==0 ? 'selected' : '' %>>Inactivo</option>
                            </select>
                        </div>

                    </div>

                    <!-- Botones -->
                    <div class="mt-4 text-end">
                        <button type="submit" class="btn btn-warning text-dark fw-semibold shadow-sm">
                            <i class="bi bi-check-lg"></i> Guardar Cambios
                        </button>
                        <a href="/publishers" class="btn btn-outline-secondary ms-2 shadow-sm">
                            <i class="bi bi-arrow-left"></i> Volver
                        </a>
                    </div>
                </form>
            </div>
        </div>

        </div>

        <%- include('../template/footer') %>