<!doctype html>
<html lang="es">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Agregar Libro</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css">
</head>

<body class="bg-light d-flex flex-column min-vh-100">

  <div class="container my-5">

    <header class="mb-4 text-center">
      <h1 class="display-5 fw-bold">Gestor de Libros</h1>
      <p class="text-muted">Utiliza el siguiente formulario para agregar un nuevo libro a tu colección.</p>
    </header>

    <% if (messages.error) { %>
      <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <%- messages.error %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Cerrar"></button>
      </div>
    <% } %>

    <!-- Formulario para agregar libro -->
    <div class="card shadow-sm">
      <div class="card-header bg-success text-white">
        <h5 class="mb-0">Formulario de Registro</h5>
      </div>
      <div class="card-body">
        <form action="/books/add" method="post">
          <div class="mb-3">
            <label for="name" class="form-label">Título del Libro</label>
            <input type="text" class="form-control" id="name" name="name" value="<%= name %>" placeholder="Ej: Cien Años de Soledad" autocomplete="off" required>
          </div>
          <div class="mb-3">
            <label for="author" class="form-label">Autor</label>
            <input type="text" class="form-control" id="author" name="author" value="<%= author %>" placeholder="Ej: Gabriel García Márquez" autocomplete="off" required>
          </div>
          <div class="text-end">
            <button type="submit" class="btn btn-primary">Agregar Libro</button>
            <a href="/books" class="btn btn-secondary ms-2">Cancelar</a>
          </div>
        </form>
      </div>
    </div>

  </div>

  <!-- Pie de página -->
  <footer class="mt-auto bg-dark text-white py-4">
    <div class="container text-center">
      <p class="mb-1">&copy; <%= new Date().getFullYear() %> Gestor de Libros. Todos los derechos reservados.</p>
      <small class="text-muted">Desarrollado con ♥ usando Bootstrap 5</small>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
